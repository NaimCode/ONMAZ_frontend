{
  /* <InView>
  {({ inView, ref, entry }) => (
    <div ref={ref}>
      <h2 className={`${inView ? "animate-spin" : ""}`}>
        Header inside viewport
      </h2>
    </div>
  )}
</InView>; */
}
